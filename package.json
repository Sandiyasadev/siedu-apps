{
    "name": "siedu",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "docker compose -f docker-compose.dev.yml up --build",
        "dev:stop": "docker compose -f docker-compose.dev.yml down",
        "dev:reset": "docker compose -f docker-compose.dev.yml down -v && docker compose -f docker-compose.dev.yml up --build",
        "dev:api": "docker compose -f docker-compose.dev.yml up api db redis",
        "dev:dashboard": "docker compose -f docker-compose.dev.yml up dashboard",
        "prod": "docker compose up --build -d",
        "prod:stop": "docker compose down",
        "db:migrate": "docker compose -f docker-compose.dev.yml exec db psql -U postgres -d siedu -f /docker-entrypoint-initdb.d/02-migration.sql"
    }
}